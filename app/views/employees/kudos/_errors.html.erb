<% if kudo.errors.any? %>
  <div class="alert alert-danger" role="alert">
    <h4 class="alert-heading"><%= pluralize(kudo.errors.count, "error") %> prohibited this kudo from being saved:</h4>
    <ul>
      <% kudo.errors.each do |error| %>
          <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
